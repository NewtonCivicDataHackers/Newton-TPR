{
  "title": "Vehicle restrictions on public ways (Sec. TPR-90)",
  "columns": ["street", "direction", "from_point", "to_point", "restriction", "vehicle_type", "exemptions", "days", "times", "location_detail", "notes", "source_text"],
  "fields": {
    "street": {
      "description": "Public way the restriction applies to (the entry's leading label, which matches the street named after 'shall go upon'/'shall enter')",
      "required": true
    },
    "direction": {
      "description": "Direction of prohibited travel or entry ('in the easterly direction' -> eastbound); empty when the source states none",
      "$ref": "common#direction"
    },
    "from_point": {
      "description": "Cross street or landmark where the restricted segment begins; empty when the restriction covers the entire way",
      "required": false
    },
    "to_point": {
      "description": "Cross street or landmark where the restricted segment ends; empty when the segment is open-ended or the restriction covers the entire way",
      "required": false
    },
    "restriction": {
      "description": "Prohibited act: 'travel' when the source says 'shall go upon' (no traveling on the way), 'entry' when it says 'shall enter' (no entering the way)",
      "required": true,
      "enum": ["travel", "entry"]
    },
    "vehicle_type": {
      "description": "Class of vehicle restricted; 'all' for 'No vehicle of any description' (currently every entry)",
      "required": true
    },
    "exemptions": {
      "description": "Exempt parties from the 'except those of ...' clause, verbatim minus the leading 'those of' and the trailing comma; empty when the prohibition is unconditional",
      "required": false
    },
    "days": {
      "description": "Days the restriction applies; 'school days' and 'when public schools of the city are in session' both normalize to school_days; empty means every day",
      "$ref": "common#days"
    },
    "times": {
      "description": "Time window(s) the restriction applies; empty means at all times",
      "$ref": "common#time_ranges"
    },
    "location_detail": {
      "description": "Relational segment qualifier from the source (e.g. 'east of Bridges Avenue'), preserving which side of from_point the restriction covers",
      "required": false
    },
    "notes": {
      "description": "Extractor remarks about anomalies in the source text; never carries data",
      "required": false
    },
    "source_text": {
      "$ref": "common#source_text"
    }
  },
  "unique": ["source_text"]
}
